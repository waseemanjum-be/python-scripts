# Random Python Scripts for day to day Usage

## Each directory contains `script.py` file and `requirements.txt` file

Install the requirements and run the script with required paramters. No complex usages
